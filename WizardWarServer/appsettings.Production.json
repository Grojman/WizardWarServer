{
  "_comment": "Kestrel terminates TLS directly using certificate.pfx at the project root (see Program.cs), so no reverse proxy is required for HTTPS. Set the CERT_PASSWORD environment variable to the pfx's password before starting the server. Override this Endpoints section / set ASPNETCORE_URLS for a different binding.",
  "Kestrel": {
    "Endpoints": {
      "Https": {
        "Url": "https://0.0.0.0:443"
      }
    }
  }
}
  